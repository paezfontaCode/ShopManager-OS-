[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --tb=short
markers =
    auth: Authentication tests
    products: Product tests
    tickets: Ticket/Sales tests
    work_orders: Work order tests
    parts: Parts tests
